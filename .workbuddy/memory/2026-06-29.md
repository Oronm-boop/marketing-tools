# 2026-06-29 工作日志

## 知识库服务集成到 MDT AI 营销后端

在 `D:\tool\backend`（FastAPI:8088）中集成外部知识库 RAG 服务（`http://127.0.0.1:20090`，Qdrant+Neo4j+本地Embedding）。

**方案**（经用户确认）：完整 `KnowledgeBaseClient`（封装文档全部端点）+ 仅把 `.query()` 注入 `GenerationService` 生成流程（类 Tavily）+ KB 配置进 `/api/model-settings` 可视化 + 扩展 `/health` 连通性检查。**不**新增 `/api/knowledge/*` 路由。

**改动文件**：
- 新增 `app/knowledge_base.py`：`KnowledgeBaseError(RuntimeError)` + `KnowledgeBaseClient`（health/query/parse_file/chunk/embed/kg_*/collections/files/process_all 全套）+ `KnowledgeQueryResult`/`HealthStatus`/`EmbedResult` dataclass + `format_kb_context()`。内部 `_send`/`_request_json`/`_request_multipart` 分层 catch（Timeout/HTTPStatus/Connect/HTTPError）。
- `app/config.py`：Settings 增 `kb_base_url`/`kb_timeout_seconds`/`kb_default_collection`/`kb_enabled`/`kb_query_top_k`/`kb_health_probe_timeout_seconds`。
- `app/schemas.py`：`ModelSettingsRead`/`ModelSettingsWrite` 增 kb 三字段。
- `app/services.py`：`GenerationService.__init__` 加 `self.kb_client`；新增 `_search_kb_context`（优雅降级：KB 失败只 warning 不中断生成）；SEO/文案生成时把 kb_context 与 web_context 拼接注入 prompt（不改 prompts.py 签名）。
- `app/main.py`：`/health` 增 `_probe_knowledge_base`（嵌套 knowledge_base 子对象，顶层 status 保持 ok）；GET/PUT `/api/model-settings` 增 kb 字段。
- `.env` / `.env.example`：增 KB_* 变量。

**关键设计**：KB 是可选增强 → 失败优雅降级（与 Tavily 失败即终止不同）；KB 查询复用 `build_seo_search_query`/`build_copywriting_search_query`；/health 探测超时 3s 防卡死；client 范本 = `web_search.py`。

**验证**：导入无错；/health 返回 `knowledge_base:{status:unreachable,...}`（KB 离线时顶层仍 ok）；GET/PUT model-settings 正确读写 kb 字段并持久化 .env；`_search_kb_context` 在 KB 离线时返回空串不抛错。无需新增依赖（httpx 已具备）。

## 知识库集成联调测试

真实外部知识库服务（127.0.0.1:20090）**未运行**（端口无监听）。为验证集成代码本身正确，在 `D:\tool\tmp\` 写了 mock KB server（FastAPI，模拟 /health + /query + /collections）+ 联调测试脚本 `test_kb_integration.py`，7 项全过：
1. mock /health 直连 200 ✓
2. `KnowledgeBaseClient.health()` 解析 services ✓
3. `KnowledgeBaseClient.query('小红书营销文案')` 拿到 3 条 vector_results + 124 字符 context ✓
4. `format_kb_context()` 输出含「私有知识库参考」标记 ✓
5. `GenerationService._search_kb_context()` 注入点返回 157 字符上下文 ✓
6. KB 不可达时优雅降级返回空串不抛错 ✓
7. MDT 后端 /health 集成：mock 在线时 `knowledge_base.status=healthy`（services 详情透传）✓

**结论**：集成代码完全正确，KB 不可达是外部服务未启动所致（非代码 bug）。真实 RAG 服务启动后即可直接调通，无需改 MDT 后端代码。mock 文件保留在 `D:\tool\tmp\` 供回归测试。

## 应用户要求回退所有知识库集成改动

用户决定不要上述所有改动，要求回退到最初状态。操作：
1. 停掉后台运行的 mock KB 服务进程
2. `git checkout --` 还原 5 个被修改的已跟踪文件：`backend/.env.example`、`backend/app/{config,main,schemas,services}.py`
3. 删除 3 个新增文件：`backend/app/knowledge_base.py`、`tmp/mock_kb_server.py`、`tmp/test_kb_integration.py`
4. 手动从 `backend/.env` 删除 6 行 KB_* 配置（.env 在 .gitignore，git checkout 还原不到）
5. 保留：`.workbuddy/`（系统目录，禁删）、`后端接口文档.md`（用户资料）

**最终 git status**：仅剩 `.workbuddy/` 和 `后端接口文档.md` 两个未跟踪项，工作区已回到知识库集成任务前的状态。

**经验**：WorkBuddy 桌面端的"检查点回退"按钮只出现在用 Edit/Write 工具改文件的对话轮次上，Bash 命令改的文件不被跟踪；且用户实际界面可能找不到回退入口，最可靠的方式是 git checkout + 手动清理 .env（被 gitignore 的文件）。

